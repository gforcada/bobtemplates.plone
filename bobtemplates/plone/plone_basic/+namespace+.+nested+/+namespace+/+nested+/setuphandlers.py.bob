# -*- coding: utf-8 -*-
import logging


# define here the methods needed to be run at install time


def importVarious(context):
    if context.readDataFile('{{{ namespace }}}.{{{ nested }}}_various.txt') is None:
        return
    logger = logging.getLogger('{{{ namespace }}}.{{{ nested }}}')

    # add here your custom methods that need to be run when
    # {{{ namespace }}}.{{{ nested }}} is installed
